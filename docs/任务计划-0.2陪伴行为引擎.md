# 0.2 陪伴行为引擎任务计划

## 一、目标

在不改变桌面陪伴定位和数据隐私边界的前提下，建立可扩展、可停止、可恢复的输入与行为基础，使键盘、鼠标和手柄反馈能够逐步从直接驱动模型迁移为统一的陪伴行为。

本阶段版本基线为 `0.2.0`。不进入选择式互动、长期记忆、在线服务或自由文字聊天。

## 二、任务状态

| 编号  | 任务                     | 状态   | 验收标准                                                   |
| ----- | ------------------------ | ------ | ---------------------------------------------------------- |
| BE-01 | 建立统一瞬时输入事件契约 | 已完成 | 键盘、鼠标和手柄事件使用同一语义模型，现有模型反馈保持一致 |
| BE-02 | 完善输入监听生命周期     | 待开始 | 监听可启动、停止、重复启动和失败恢复，退出时释放资源       |
| BE-03 | 合并高频鼠标事件         | 待开始 | 鼠标移动按渲染节奏合并，不逐条进入行为层                   |
| BE-04 | 建立瞬时活动状态         | 待开始 | 仅在内存维护活跃、空闲、输入强度和最近活动状态             |
| BE-05 | 实现三种陪伴模式         | 待开始 | 安静、陪伴和活跃模式具有明确且可切换的行为约束             |
| BE-06 | 实现行为调度             | 待开始 | 支持优先级、打断、恢复、冷却和频率上限                     |
| BE-07 | 实现有限范围移动         | 待开始 | 宠物只在偏好位置附近移动，尊重锁定和屏幕边界               |
| BE-08 | 完成 Windows 与隐私验收  | 待开始 | Windows 实机稳定运行，磁盘和日志无原始输入事件             |

状态取值：`待开始`、`进行中`、`已完成`、`阻塞`。

## 三、实施顺序

1. 定义平台无关的输入事件语义，并在前端入口完成归一化。
2. 修正 Rust 输入监听的启动、停止、并发和失败恢复行为。
3. 将鼠标移动合并到受控频率，再提供给行为状态层。
4. 建立不持久化的活动、精力和好奇心等瞬时状态。
5. 在状态层之上实现陪伴模式和行为调度。
6. 最后接入有限移动，并完成 Windows 性能、稳定性和隐私验收。

## 四、数据与隐私边界

- 统一事件只存在于运行内存，不进入 Pinia 持久化 Store、日志、缓存或崩溃报告。
- 具体按键、鼠标坐标、手柄按钮和单次事件时间不得持久化。
- 行为层只消费当前状态或短时间聚合结果，不保存可还原操作过程的序列。
- 本阶段不新增长期数据字段；未来需要聚合统计时另行设计保留周期、查看和删除方式。
- 暂停输入监听后必须清除按下状态、移动目标和尚未消费的瞬时事件。

## 五、完成定义

- 三类输入具有统一、可测试的平台无关事件契约。
- 输入监听具备明确生命周期，不出现重复监听或空转占用。
- 高频输入不会直接造成渲染抖动或无界队列积压。
- 三种陪伴模式和行为调度均有自动化检查与 Windows 实机证据。
- 磁盘、日志和持久化 Store 中不存在具体按键、鼠标轨迹或单次输入事件。

## 六、当前实施结果

### 6.1 已确认现状

- Rust 已分别采集键鼠和手柄事件，并通过两个 Tauri 事件发送到前端。
- 前端 `useDevice` 与 `useGamepad` 目前分别将原始载荷直接映射为 Live2D 参数和按键贴图。
- 鼠标位置已经按 PixiJS ticker 平滑消费，但尚未形成统一输入语义和行为状态。
- 键鼠监听只能暂停事件发送，手柄监听使用独立循环，两者的生命周期语义尚不一致。

### 6.2 首批范围

- 已将版本基线提升到 `0.2.0`。
- 已新增不持久化的统一输入事件类型与归一化函数。
- 键盘、鼠标和手柄入口已改为先归一化，再复用现有模型反馈逻辑。
- 已增加输入归一化单元测试，不在测试或生产日志中记录真实用户输入。

### 6.3 验证结果

- `corepack pnpm test:input`：3 项输入归一化测试通过。
- `corepack pnpm lint`：通过。
- `corepack pnpm build`：通过；仍存在主包体积超过 500 kB 的既有警告。
- `cargo fmt --all -- --check`：通过。
- `cargo check --workspace --locked`：通过。
- Windows `corepack pnpm tauri dev`：`v0.2.0` 启动成功，进程响应正常。

### 6.4 下一任务

- 开始 BE-02，统一键鼠与手柄监听的启动、停止、重复启动和失败恢复语义。
- 重点处理手柄监听循环的资源占用，以及应用退出或切换模式时的可靠释放。
