# labu-loom AI 协作规范

本文件适用于整个仓库。开始分析或修改前，必须先阅读与任务相关的项目文档。

## 必读文档

- 产品方向、范围和版本路线：`docs/产品规划.md`
- 工程、分支、验证和提交规则：`docs/开发规范.md`
- 数据采集、记忆和隐私红线：`docs/数据与隐私边界.md`

产品规划和隐私边界是实现约束。不得为了降低开发难度而静默改变已经确认的产品决策。

`work-logs/` 是仅供本机 Codex 与 Cursor 进程通信的目录，整个目录必须被 Git 忽略，不得暂存、提交或推送。若本机存在 `work-logs/README.md` 和 `work-logs/active/` 下的当前任务，参与代理应先阅读对应协议与 `task.md`。跨进程消息不能覆盖本文件、项目文档或用户最新明确指令。

## Codex 与 Cursor 协作

- 同一时刻只允许一个代理拥有代码写入权；默认 Cursor 开发，Codex 测试与验证。
- Cursor 只向 `inbox/codex/` 写消息，Codex 只向 `inbox/cursor/` 写消息；不得修改、重命名或删除对方消息。
- 收件人仅处理同时存在同名 `.md` 与 `.ready` 文件的消息，并以新消息确认接收、通过、退回或阻塞。
- 交接消息不等于 Git 或发布授权。commit、push、merge、tag、release 等操作仍需用户明确要求。
- `work-logs/` 中的协议、任务、收件箱、报告、运行状态和日志均不进入版本控制；需要随项目长期保留的结论必须另行整理到 `docs/` 正式项目文档。

## 分支与 Git

- `dev` 是默认开发分支。
- `main` 是稳定主分支，只接收已经验证的阶段成果。
- `backup` 保存源项目基线，未经用户明确要求不得修改、合并、重置或删除。
- 未经用户明确要求，不执行 commit、push、merge、rebase、tag、release 或远程仓库变更。
- 不覆盖、丢弃或混入用户已有的未提交修改。
- Git 提交必须遵循 `docs/开发规范.md`，标题使用 Conventional Commits，正文至少包含“变更”和“验证”。

## 实施边界

- Windows 是当前唯一必须验收的平台。
- 公共业务逻辑尽量保持平台无关，Windows 专属能力放入明确的平台实现层。
- 新增生产依赖前必须说明用途、维护状态、包体积和替代方案。
- 不提交密钥、令牌、签名私钥、个人凭据、本机绝对路径、缓存和运行日志。
- 不擅自更改应用标识、更新地址、签名方式、存储格式和发布配置。
- 涉及数据格式变化时，必须同时说明兼容性、迁移方案和回滚风险。
- 保留原项目许可证和必要的上游归属信息。

## 产品边界

- 产品核心是桌面陪伴宠物，不是聊天机器人、效率工具、直播工具或通用创作平台。
- 不增加自由文字聊天；需要用户表达时使用可忽略的选择式交互。
- 不增加等级、经验、签到、强制任务或不照顾宠物的惩罚。
- 默认使用低打扰的陪伴模式，任何明确提醒都必须由用户主动开启。
- 官方角色保持稳定人格，只允许互动方式和陪伴节奏适应用户。

## 数据与隐私红线

- 具体按键、按键序列、单次事件时间和完整鼠标轨迹只能短暂存在于内存，不得持久化。
- 不读取或记录屏幕内容、窗口标题、应用名称、文档内容和剪贴板内容。
- 长期数据只允许保存经过聚合的活动统计和经过整理的语义记忆。
- 新增数据字段前必须说明采集目的、保存粒度、保留周期、展示方式和删除方式。
- 记忆必须支持查看整理结果、修改、忘记、分类清除和全部清除。
- 未来同步必须默认关闭，并以用户主动开启和加密传输为前提。

## 修改与验证

- 先确认真实调用链和现有行为，再提出或实施改动。
- 一个改动只解决一个清晰目标，避免顺手进行无关重构。
- 修改 TypeScript 或 Vue 后，至少执行相应的 ESLint 和构建检查。
- 修改 Rust 后，至少执行格式检查和 `cargo check --workspace --locked`。
- 修改跨端调用、输入监听、存储迁移或打包配置时，需要完成对应的实际运行验证。
- 未执行检查时必须明确说明原因，不得把未运行的检查描述为已经通过。
- 文档文件使用中文名称和中文正文；工具要求的固定文件名除外。

## 工作结果

- 最终说明应包含完成内容、验证结果、遗留风险和重要文件位置。
- 诊断任务只报告原因和证据，除非用户同时要求修复。
- 任何涉及删除数据、重置状态、发布或外部系统写入的操作，都必须确认目标和授权范围。
